const primalArray = [ 0, 1, 23, 84, 1];
// função para encontrar a posição de um elemento num array
console.log(primalArray.indexOf(23));
// return 2

// encontrando a última aparição de um elemnto numa array
console.log(primalArray.lastIndexOf(1));
// return 4

// verficando se determinado elemnto existe uma array
console.log(primalArray.includes(25));
// return false não há elemento 25 no array
